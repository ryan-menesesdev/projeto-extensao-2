<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalhes do Pedido</title>
    <style>
        body { font-family: sans-serif; padding: 20px; }
        .details, .products { border: 1px solid #ccc; padding: 15px; border-radius: 5px; margin-bottom: 20px; }
        h1, h2 { color: #333; }
        ul { list-style: none; padding: 0; }
        li { background-color: #f4f4f4; margin-bottom: 5px; padding: 10px; border-radius: 5px; }
        a { display: inline-block; margin-top: 20px; color: #007bff; }
    </style>
</head>
<body>

    <% if (typeof order !== 'undefined') { %>
        
        <h1>Detalhes do Pedido #<%= order.details.id %></h1>

        <div class="details">
            <h2>Informações Gerais</h2>
            <p><strong>Cliente:</strong> <%= order.details.nomeCliente %></p>
            <p><strong>Email:</strong> <%= order.details.emailCliente %></p>
            <p><strong>Status do Pedido:</strong> <%= order.details.statusPedido %></p>
            <p><strong>Método de Pagamento:</strong> <%= order.details.metodoPagamento %></p>
            <p><strong>Status do Pagamento:</strong> <%= order.details.statusPagamento %></p>
        </div>

        <div class="products">
            <h2>Produtos no Pedido</h2>
            <ul>
                <% order.products.forEach(product => { %>
                    <li>
                        <strong>Produto:</strong> <%= product.nome %> <br>
                        <strong>Quantidade:</strong> <%= product.quantidade %> <br>
                        <strong>Preço Unitário:</strong> R$ <%= product.preco.toFixed(2) %>
                    </li>
                <% }) %>
            </ul>
        </div>

    <% } else { %>
        <h1>Pedido não encontrado.</h1>
    <% } %>
    
    <a href="/admin/orders">← Voltar para a lista de pedidos</a>

</body>
</html>