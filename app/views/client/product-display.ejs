<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="description" content="Veja os detalhes dos nossos produtos, incluindo descrição e preço, na Sinhá Bolos e Lanches.">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" integrity="sha512-SnH5WK+bZxgPHs44uWIX+LLJAJ9/2PkPKZ5QiAj6Ta86w+fsb2TkcmfRyVX3pBnMFcV7oQPJkl9QevSCWr3W6A==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="icon" type="image/png" href="/images/logo-better-nobackground.png">
    <link rel="stylesheet" href="/css/reset.css">
    <link rel="stylesheet" href="/css/back.css">
    <link rel="stylesheet" href="/css/item-display.css"> 
    <link rel="stylesheet" href="/css/footer.css">
    <link rel="stylesheet" href="/css/header.css">
    <title><%= product ? product.nome : 'Detalhes do Produto' %></title>
</head>
<body>
    <%- include('../partials/header') %>

    <main>
        <div class="top-container">
            <a class="top-link" href="javascript:history.back()" aria-label="Voltar para a página inicial">
                <i class="fas fa-arrow-left" aria-hidden="true"></i>
                <span>Voltar</span>
            </a>
        </div>
        <div class="item-display-main-container">
            <% if (product) { %>
                <article class="item-display-section" id="product-detail-container" aria-live="polite" aria-labelledby="product-title">
                    <div class="item-display-image-container">
                        <img 
                            class="item-display-image" 
                            src="<%= product.imagem || '/images/avatar_placeholder.png' %>" 
                            alt="<%= product.nome %>"
                        >
                    </div>

                    <div class="item-display-info-container">
                        <h1 class="item-display-title" id="product-title"><%= product.nome %></h1>
                        
                        <p class="item-display-category">
                            <strong>Categoria:</strong> 
                            <span class="category-badge"><%= product.categoria %></span>
                        </p>

                        <p class="item-display-description">
                            <strong>Descrição:</strong> 
                            <%= product.descricao %>
                        </p>

                        <div class="item-display-price-container">
                            <p class="item-display-price" aria-label="Preço do produto">
                                <strong>Preço:</strong> 
                                <span class="price-value">
                                    <%= parseFloat(product.preco).toLocaleString('pt-BR', { 
                                        style: 'currency', 
                                        currency: 'BRL' 
                                    }) %>
                                </span>
                            </p>
                        </div>

                        <div class="item-display-availability">
                            <p class="item-display-availability-status">
                                <strong>Disponibilidade:</strong> 
                                <span class="<%= product.disponivel ? 'availability-available' : 'availability-unavailable' %>">
                                    <%= product.disponivel ? '✓ Disponível' : '✗ Indisponível' %>
                                </span>
                            </p>
                        </div>

                        <% if (product.disponivel) { %>
                            <div class="item-display-actions">
                                <form action="/cart/add" method="post">
                                    <input type="hidden" name="productId" value="<%= product.id %>">
                                    <button 
                                        class="item-display-button" 
                                        data-product-id="<%= product.id %>"
                                        aria-label="Adicionar <%= product.nome %> ao carrinho"
                                    >
                                        <i class="fas fa-shopping-cart" aria-hidden="true"></i>
                                        Adicionar ao Carrinho
                                    </button>
                                </form>
                            </div>
                        <% } else { %>
                            <div class="item-display-actions">
                                <button 
                                    class="item-display-button unavailable-btn" 
                                    disabled
                                    aria-label="Produto indisponível"
                                >
                                    <i class="fas fa-ban" aria-hidden="true"></i>
                                    Indisponível
                                </button>
                            </div>
                        <% } %>

                        <div class="item-display-info">
                            <p class="item-display-info-text">
                                <i class="fas fa-info-circle" aria-hidden="true"></i>
                                Para adicionar este produto ao carrinho, você deve estar logado em sua conta.
                            </p>
                        </div>
                    </div>
                </article>
            <% } else { %>
                <article class="item-display-section" id="product-detail-container" aria-live="polite">
                    <div class="error-container">
                        <div class="error-icon">
                            <i class="fas fa-exclamation-circle" aria-hidden="true"></i>
                        </div>
                        <h1 class="error-title">Produto não encontrado</h1>
                        <p class="error-message">Desculpe, o produto que você está procurando não está disponível.</p>
                        <a href="/" class="error-link-button">
                            <i class="fas fa-arrow-left" aria-hidden="true"></i>
                            Voltar à página inicial
                        </a>
                    </div>
                </article>
            <% } %>
        </div>
    </main>

    <%- include('../partials/footer') %>

    <script src="/js/utility.js" type="module"></script>
</body>
</html>